<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidTakeHome" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="CustomToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="titleTextAppearance">@style/CustomToolbarTitleText</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <!-- Custom Toolbar Title Text Appearance -->
    <style name="CustomToolbarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">25sp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ToolbarColoredBackArrow" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlNormal">@android:color/white</item>
    </style>


    <style name="CustomItemNameText">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/action_bar</item>
    </style>

    <style name="CustomListNameText">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/action_bar</item>
        <item name="android:textStyle">bold</item>
    </style>



    <style name="Theme.AndroidTakeHome" parent="Base.Theme.AndroidTakeHome" />
</resources>